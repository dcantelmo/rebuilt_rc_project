<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
        <link
            rel="stylesheet"
            href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
        />

        <script>
            function roomName() {
                var url = new URL(window.location.href);
                if (url.searchParams.get("roomName")) {
                    document.form.id.value = url.searchParams.get("roomName");
                }
            }
        </script>

        <title>Join a room</title>
    </head>

    <body onload="roomName();">
        <h1>Pagina JOIN ROOM del sito</h1>
        <br />
        <h2>Accedi o crea una room</h2>
        <form action="<%= room_link %>" method="POST" name="form">
            <input
                type="text"
                size="20"
                maxlength="10"
                name="id"
                placeholder="Room Name"
            />
            <input
                type="password"
                size="20"
                name="password"
                placeholder="Insert room password"
            />
            <input type="submit" value="invia" />
        </form>
        <%if (error) { %>
        <h4><%= error %></h4>
        <% } %>
        <br /><br />
    </body>
</html>
