<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/vue/dist/vue.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.3.0/socket.io.js"></script>
    <link rel="stylesheet" href="/public/css/draw.css">
<body>
    <div id="app" room="<%= ROOM %>" pass="<%= PASSWORD %>" username="<%= USERNAME %>">
        <Vueword :socket="socket"></Vueword>
        <Vuecanvas v-if="socket" ref="myCanvas" :mode="canvasMode" :key="canvasMode" :socket="socket"></Vuecanvas>
        <input type="color" @change="changeColor($event)" pattern="#[0-9A-Fa-f]{6}" placeholder="Hex Color in this format -> #000000"/>
        <button @click="change()">{{canvasMode}}</button>
        <button v-if="canvasMode == 'drawer'" id='start' @click="start($event)">Start</button>
        <h4>Timer: {{timer}}</h4>
        <Vuechat :socket="socket" />
    </div>
    <script src="/public/js/draw.js"></script>
</body>
</html>